# Copy this file to `.env` in the project root (same level as package.json).
# Then set your public backend URL for phones anywhere:
# Example: API_BASE_URL=https://api.yourdomain.com/api

API_BASE_URL=
